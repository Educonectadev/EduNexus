FROM node:20-alpine

WORKDIR /app

COPY package.json pnpm-lock.yaml ./
RUN corepack enable && pnpm install --frozen-lockfile

COPY . .

RUN npx tsx --version > /dev/null 2>&1

EXPOSE 3001

CMD ["npx", "tsx", "server.ts"]
